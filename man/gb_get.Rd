% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gb-get.R
\encoding{UTF-8}
\name{gb_get}
\alias{gb_get}
\title{Get individual country boundary files from geoBoundaries}
\source{
geoBoundaries API Service \url{https://www.geoboundaries.org/api.html}.
}
\usage{
gb_get(
  country,
  adm_lvl = "adm0",
  simplified = FALSE,
  release_type = c("gbOpen", "gbHumanitarian", "gbAuthoritative"),
  quiet = TRUE,
  overwrite = FALSE,
  path = NULL
)
}
\arguments{
\item{country}{Character vector of country identifiers. This can be \code{"all"}
(to return data for all countries), a vector of country names, or ISO3
country codes. See also \code{\link[countrycode:countrycode]{countrycode::countrycode()}}.}

\item{adm_lvl}{Type of boundary to retrieve. Accepted values are \code{"all"} (all
available administrative levels) or a specific ADM level. \code{"adm0"}
corresponds to country boundaries, \code{"adm1"} to first-level subnational
boundaries, \code{"adm2"} to second-level boundaries, and so on. Uppercase
variants (e.g. \code{"ADM1"}) and numeric values (\code{0}, \code{1}, \code{2}, \code{3}, \code{4}, \code{5})
are also accepted.}

\item{simplified}{logical. If \code{TRUE}, return simplified boundaries. If
\code{FALSE} (the default), use the premier geoBoundaries release.}

\item{release_type}{One of \code{"gbOpen"}, \code{"gbHumanitarian"}, or
\code{"gbAuthoritative"}. For most users, \code{"gbOpen"} (the default) is
recommended, as it is CC-BY 4.0 compliant and suitable for most purposes
provided proper attribution is given:
\itemize{
\item \code{"gbHumanitarian"} files are mirrored from
\href{https://www.unocha.org/}{UN OCHA} and may have more restrictive
licensing.
\item \code{"gbAuthoritative"} files are mirrored from UN SALB and cannot be used
for commercial purposes, but are verified through in-country processes.
}}

\item{quiet}{logical. If \code{TRUE}, suppress informational messages.}

\item{overwrite}{logical. If \code{TRUE}, force a fresh download of the source
\code{.zip} file, even if it is already cached.}

\item{path}{Character. Path to a cache directory. If \code{NULL} (the default),
data are stored in the default cache directory (see \code{\link[=gb_set_cache]{gb_set_cache()}}). If
no cache directory has been configured, files are stored in a temporary
directory (see \code{\link[base:tempfile]{base::tempdir()}}).}
}
\value{
An \code{\link[sf:sf]{sf}} object containing the requested boundaries.
}
\description{
\href{https://www.geoboundaries.org/index.html#usage}{Attribution} is required for
all uses of this dataset.

This function returns boundary data for individual countries \emph{as they
represent themselves}, without any special identification of disputed areas.

If you require data that explicitly includes disputed areas, use
\code{\link[=gb_get_world]{gb_get_world()}} instead.
}
\details{
Individual boundary files in the geoBoundaries database are governed by the
license or licenses specified in their associated metadata (see
\code{\link[=gb_get_metadata]{gb_get_metadata()}}). Users working with individual boundary files should
ensure that they comply with these licenses and cite the original data
sources listed in the metadata. See \strong{Examples}.

The following convenience wrappers are also available:
\itemize{
\item \code{\link[=gb_get_adm0]{gb_get_adm0()}} returns country boundaries.
\item \code{\link[=gb_get_adm1]{gb_get_adm1()}} returns first-level administrative boundaries
(e.g. states in the United States).
\item \code{\link[=gb_get_adm2]{gb_get_adm2()}} returns second-level administrative boundaries
(e.g. counties in the United States).
\item \code{\link[=gb_get_adm3]{gb_get_adm3()}} returns third-level administrative boundaries
(e.g. municipalities in some countries).
\item \code{\link[=gb_get_adm4]{gb_get_adm4()}} returns fourth-level administrative boundaries.
\item \code{\link[=gb_get_adm5]{gb_get_adm5()}} returns fifth-level administrative boundaries.
}
}
\examples{
\dontshow{if (httr2::is_online()) withAutoprint(\{ # examplesIf}

\donttest{
# Level 2 administrative boundaries in Sri Lanka
sri_lanka <- gb_get(
  "Sri Lanka",
  adm_lvl = 2,
  simplified = TRUE
)

sri_lanka

library(ggplot2)
ggplot(sri_lanka) +
  geom_sf() +
  labs(caption = "Source: www.geoboundaries.org")
}

# Inspect metadata and licensing
library(dplyr)
gb_get_metadata(
  "Sri Lanka",
  adm_lvl = 2
) |>
  select(boundaryISO, boundaryType, licenseDetail, licenseSource) |>
  glimpse()
\dontshow{\}) # examplesIf}
}
\references{
Runfola, D. et al. (2020) geoBoundaries: A global database of political
administrative boundaries. \emph{PLoS ONE} 15(4): e0231866.
\doi{10.1371/journal.pone.0231866}.
}
\seealso{
Other API functions: 
\code{\link{gb_get_adm}},
\code{\link{gb_get_world}()}
}
\concept{API functions}
